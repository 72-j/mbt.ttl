// gen_guard：**跨包结构钉子包**（T17 / C-T8 的"源同步钉子"与"裁定表钉子"，2026-09-20 立）
//
// 为什么需要独立包：钉子要同时看 `gen_shared`（抽件名单）与两个方言包（防回潮 / 分叉项存在），
// 而 `gen_shared` 不能反向依赖方言包（成环）⇒ 钉子住在第三处：**只读源文件文本**做断言。
//
// 性质：本包**不进任何产品面**（只有钉子与名单常量）；断言 = 结构钉子 + 反向断言
//（世界宪法 §6.2.2：移出/分叉必须钉住，且该事实反转即红）。

import {
  "moonbitlang/async",
  "moonbitlang/async/fs",
} for "wbtest"
