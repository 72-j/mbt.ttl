// n3gen：N3 FSM 声明面词表编译器（v2 emit 直产，ADR §5.14 役15）
// 声明源 n3_base.toml + n3_trans.toml → parse → validate → emit n3.mbt
// 产物逐字节对拍 check-in ../gen_n3/n3.mbt（G0 黄金门）；fsm cmd 自 n3 管线除役
import {
  "hnlyxiaobing/toml",
}

import {
  "moonbitlang/async",
  "moonbitlang/async/fs",
  "moonbitlang/core/encoding/utf8",
} for "test"
