# 覆盖率基线（ratchet：CI 只许 ≥ 本值，不许倒退）
#
# 度量口径（CI static 作业同款，固定顺序保证可复现）：
#   moon clean && moon coverage clean && moon test --enable-coverage && moon coverage report -f summary | tail -1
#   （`moon clean` 不可省：源码改动后 _build 里会留下**旧插桩产物**，
#     与新产物混跑会写出对不上的 trace ⇒ 报告直接 `Counts array length mismatch`
#     甚至 `Invalid_argument("index out of bounds")`。2026-09-18 实测踩过。）
#
# 纪律：基线是**记账**不是目标——补测试把线推上去后，同笔更新本文件（并在本仓 todo.md 记一笔，
#       例：§AI = 三字段追补）；
# 要下调必须写明理由（例如删了一整块被测代码 / 覆盖率统计口径变化）。
#
# 口径（写死）：covered_permille = floor(covered_lines * 1000 / total_lines)——**整数取整**，CI 同式。
#   四舍五入会差 1‰（实测：9096/10460 取整 = 869，四舍五入 = 870）；
#   推基线必须用取整值，照四舍五入值推会让棘轮立刻变红。
#
# 2026-09-18 立基（役 1 后补测试：68% → 82.3%）→ 第二批役（2/3/4/5/6/14）83.2%
# → 役 7（引擎侧普查 + 观测面桥/状态名枚举）83.4%
# → R-18（P5.6 笔 1/2：n3v2 同源翻转 + quicktest 词表接深切片，model_exec 376→384 臂
#   全被全臂冒烟覆盖、bridge/状态名收编两新态）86.4%
# → 笔 3a（R-17 深半役：path 三账值级建模——quad_slots 隔离/fwd: 写源/折写压帧/
#   IR assign 通道 28 行；frame_open_span 用途门清死码；符号期全矩阵扫锚点点亮
#   3a 内联化的 53 处 None 半区）86.8%
# → 3b 笔 2 中间态（帧扩展位数据化 + `path_elem_end` 词表行；债点仍 6，见 todo §S）86.9%
# → 3b 笔 2 收账（帧字段读写助手按字段发 + 集合族/元素位词表补全 ⇒ **债点 6 → 2**，见 §T）87.0%
# → 3c 上半（值分支臂 `when_absent`/`when_some` 数据化 + `path_fwd`/`path_bwd`/`path_subj_end`
#   按引擎真值补全；余债 2 见 §U）86.9%
#   **下调说明（870 → 868）**：分母 9996 → 10068（值分支臂是把两条机械**内联**进同一臂 ⇒ 行数增加），
#   分子 8705 → 8745；新臂里的"另一支"在当前权重下走不到 ⇒ 短期覆盖率被稀释。
#   债点清完后（排除片放宽、权重上调）这两支会自然被走到，基线随之上推。
# → 3c 下半·二（罩内发射压制开通：`suppress_emit` + 压帧即定旗 + Bool 位字面写；
#   **债点 2 → 0**，见 §V.4）86.7%
#   **下调说明（868 → 867）**：分母 10068 → 10080（emit_* 罩内压制首行/帧位尾参/
#   Bool 字面写分支等 +12 行），分子 8745 持平——新增机械**全部被走到**
#   （普查/随机门执行即计），只是分母稀释了一个千分位。债清权重放开后随上推。
# → 笔 4（役 9 nit 生成器物化 + 覆盖率门范围断言）后实测 8782/10114 = 868‰（门上限未动，分数线自然回升）
# → 2026-09-19 追补（**与"可达覆盖门落地"同笔**；本笔只做三字段复算 + 取整口径写死，不动其他门）：
#   `moon clean` 口径**冷跑两次**均为 **9193/10550 = 871‰**（floor；round 871）。
#   三字段此前停在 8745/10080（滞后约 400 行；期间新臂面/新门自身被测试覆盖 ⇒ 分子分母同步增长），
#   判据字段 868 → **871** 同笔推高。中间读数 9096/10460 = 869‰（同日晚，追补前的库面）仅存沿革。
→ 2026-09-20 追补（**役 A「门自证 + 覆盖率回补」同笔**）：
  役 A 开题实测：基线钉已滞后树面——冷跑 9611/11064 = 868‰（<871）。按 §6.1.4 本应留痕下调，
  但根因不是测试退化：Batch 6 倒装机械入账后基线未随跑复算（分母 10550 → 11064 未记账）。
  役 A 补测后冷跑 **10029/11072 = 905‰**（floor；round 905）⇒ 判据字段 871 → **905** 同笔推高。
  本役点亮两族：①数据面门自证（n3 17 + trig 12 白盒/负控——直调私有函数，红-only 防御臂与
  消息渲染面入分子，每条负控断言"确实红 + 理由文本对得上"）；
  ②结构性 0 覆盖裁决·补测路（bench/cmd/examples 四个 main 提取冒烟入口 wbtest 直调 +
  gen_nquads Token Show 十臂钉 ≈ 250 行入分子；argv/env 进程态胶水 ~10 行结构性不可宿主，
  如实留残差，见 review.md §1 注）。
→ 2026-09-20 同日追补（**HOLD 族转正役同笔复算**）：
  Invert 真臂化（set_inversion 升全形声明，model_exec +2 行净增）⇒ 分母 11072 → 11074、
  分子 10029 → 10030，floor(10030*1000/11074) = 905 ⇒ 判据字段 905 **保持**（复算留痕，非下调）。
# 2026-09-20 **T17 双包抽件**（C-T8）：14 项纯函数（≈445 行/包）并入新包 `src/gen_shared` ⇒ 重复副本归一：
#   分母 11074 → **10906**（−168）、分子 10030 → **9887**（−143），floor(9887*1000/10906) = **906**
#   ⇒ 判据字段 905 → **906 同笔推高**（抽件顺带固化：去重复分母，非调数）。
→ 2026-09-20 R-17 转正复算（Id 事件建模四面手术同笔）：
  set_id_subject 真臂化（model_exec 真臂 + 条件帧写助手 +8 行）+ 数据面 `flag:` 条件式
  分类器 + 可达性 BFS 帧感知签名（去重键 = (态, 帧顶 ret_state)）
  ⇒ 分母 10906 → **10965**、分子 9887 → **9937**，floor(9937*1000/10965) = **906**
  ⇒ 判据字段 906 **保持**（复算留痕，非调数；普查 2255 对 / 数据面 314 行零违例随门自证）。
covered_permille = 906
covered_lines = 9937
total_lines = 10965
